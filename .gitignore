# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/

# CocoaPods — Podfile.lock IS committed; the Pods dir is not
Pods/

# Conductor agent workspace
.context/

# Local agent context; keep public repos free of private project/security notes.
CLAUDE.md
.gstack/

# Server
node_modules/
*.log
.env

# Local-only build config — never commit real relay endpoints / infra / secrets
*.local.xcconfig
Local.xcconfig
secrets.xcconfig

# Reproducible WebRTC build output (published as a release artifact, never committed)
.webrtc-build/
Dependencies/TojWebRTC/WebRTC.xcframework/
Dependencies/TojWebRTC/WebRTC.xcframework.zip
Dependencies/TojWebRTC/WebRTC.xcframework.zip.sha256

# Coturn deployment secrets and certificates
infra/coturn/turnserver.conf
infra/coturn/certs/
