{
  "name": "toj-dev-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "cloud": "bun run src/cloud.ts",
    "worker:scheduled-delivery": "bun run src/scheduled-delivery-worker.ts",
    "worker:link-preview": "bun run src/link-preview-worker.ts",
    "migrate": "bun run src/migrate.ts",
    "test:migration:forward": "bun run scripts/test-message-forward-migration.ts",
    "benchmark:sync-lifecycle": "bun run scripts/benchmark-sync-lifecycle-index.ts",
    "benchmark:envelope-migration": "bun run scripts/benchmark-envelope-migration.ts",
    "backfill:saved-messages": "bun run scripts/backfill-saved-messages.ts",
    "verify:dialog-preferences-migration": "bun run scripts/verify-dialog-preferences-migration.ts",
    "moderation:reports": "bun run scripts/moderation-reports.ts",
    "crypto:keys": "bun run scripts/envelope-keys.ts",
    "backup": "bash scripts/backup-postgres.sh",
    "restore-drill": "bash scripts/restore-drill.sh",
    "test": "bun test"
  },
  "dependencies": {
    "ipaddr.js": "^2.4.0",
    "pg": "^8.16.3",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/pg": "^8.15.5"
  }
}
